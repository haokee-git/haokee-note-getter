我已详细分析了代码并制定了以下修复与改进计划：

## 1. 窗口动画与交互优化
- **弹窗动画**: 为 `CustomMessageBox` 和 `HelpDialog` 实现 `showEvent` 和 `closeEvent` 动画，使用 `QPropertyAnimation` 实现透明度淡入淡出和轻微缩放效果，告别生硬的弹出。
- **关闭按钮圆角**: 修复 QSS 样式表，确保 `closeBtn` 在悬停状态下保持右上角的圆角设计（仅在非最大化时），避免“退化成直角”。
- **窗口拖动调整大小**: 在 `MainWindow` 中实现边缘检测逻辑（重写鼠标事件），允许用户通过拖动窗口边缘来调整窗口大小。

## 2. 界面细节打磨 (UI Polish)
- **复选框**: 将“显示媒体文件”复选框的大小从 18px 缩小至 14px，使其更精致。
- **图标优化**: 调整文件夹、文件及媒体图标的颜色，使用更柔和的 Material Design 风格配色（如更暖的黄色、更清新的蓝色），提升“高雅”感。
- **布局调整**: 将“保存位置”输入框和“选择文件夹”按钮的高度统一固定（如 32px），确保视觉对齐。
- **标题修改**: 将应用标题统一改为 **"Haokee Note 下载器"**。

## 3. 新增文件预览功能
- **后端逻辑**: 修改 `FetchContentThread`，使其在抓取媒体链接的同时返回原始文本内容。
- **前端布局**: 在右侧面板的“媒体文件”列表上方新增一个 `QTextEdit` 预览区域。
- **交互逻辑**:
    - 当选中 `.md`, `.js`, `.css`, `.html`, `.txt` 等文本文件时，显示预览区域并加载内容。
    - 预览区域与媒体列表通过 `QSplitter` 分割，或自动调整比例，确保列表高度自动适应。
    - 选中非文本文件或解码失败时，自动隐藏预览区域。
